name: RALD-1 isolated implementation

on:
  push:
    branches: [rald1-impl-session-20260915]

permissions:
  contents: write

jobs:
  implement-verify-push:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rewrite/rust-core
          fetch-depth: 0
      - name: Verify frozen parent
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = 771d39f9337a7c15337fedc6b47b43d017d8140c
          git fetch origin rewrite/rust-core
          test "$(git rev-parse origin/rewrite/rust-core)" = 771d39f9337a7c15337fedc6b47b43d017d8140c
          test -z "$(git status --porcelain)"
      - name: Apply RALD-1 code and normative contract
        shell: bash
        run: |
          set -euo pipefail
          cat > crates/core/src/local_derived.rs <<'RUST'
          use super::*;

          const PROVENANCE_FORMAT: &str = "codex-local-derived-v1";
          const LOCAL_RELEASE_BASE_HOST: &str = "local.invalid";

          #[derive(Debug, Clone, PartialEq, Eq)]
          pub(super) struct PublicBaseline {
              pub(super) release_sequence: u64,
              pub(super) update_key: ReleasePublicKey,
              pub(super) release_manifest_sha256: String,
          }

          fn valid_sha256(value: &str) -> bool {
              value.len() == 64
                  && value
                      .bytes()
                      .all(|byte| byte.is_ascii_digit() || matches!(byte, b'a'..=b'f'))
          }

          pub(super) fn render_provenance(baseline: &PublicBaseline) -> String {
              format!(
                  "{PROVENANCE_FORMAT};baseline_sequence={};baseline_update_key={};baseline_manifest_sha256={}",
                  baseline.release_sequence,
                  baseline.update_key.to_hex(),
                  baseline.release_manifest_sha256
              )
          }

          pub(super) fn parse_provenance(
              value: &str,
          ) -> Result<Option<PublicBaseline>, LocalProductError> {
              if !value.starts_with(PROVENANCE_FORMAT) {
                  return Ok(None);
              }
              let fields: Vec<&str> = value.split(';').collect();
              if fields.len() != 4 || fields[0] != PROVENANCE_FORMAT {
                  return Err(LocalProductError::Release(
                      "local-derived provenance is malformed",
                  ));
              }
              let sequence_text = fields[1]
                  .strip_prefix("baseline_sequence=")
                  .ok_or(LocalProductError::Release(
                      "local-derived provenance sequence is malformed",
                  ))?;
              let release_sequence = sequence_text.parse::<u64>().map_err(|_| {
                  LocalProductError::Release("local-derived provenance sequence is malformed")
              })?;
              if release_sequence == 0 || release_sequence.to_string() != sequence_text {
                  return Err(LocalProductError::Release(
                      "local-derived provenance sequence is malformed",
                  ));
              }
              let update_key_text = fields[2]
                  .strip_prefix("baseline_update_key=")
                  .ok_or(LocalProductError::Release(
                      "local-derived provenance update key is malformed",
                  ))?;
              let update_key = ReleasePublicKey::parse_hex(update_key_text).ok_or(
                  LocalProductError::Release("local-derived provenance update key is malformed"),
              )?;
              let digest = fields[3]
                  .strip_prefix("baseline_manifest_sha256=")
                  .ok_or(LocalProductError::Release(
                      "local-derived provenance manifest digest is malformed",
                  ))?;
              if !valid_sha256(digest) {
                  return Err(LocalProductError::Release(
                      "local-derived provenance manifest digest is malformed",
                  ));
              }
              Ok(Some(PublicBaseline {
                  release_sequence,
                  update_key,
                  release_manifest_sha256: digest.to_owned(),
              }))
          }

          pub(super) fn current_is_local_derived(
              before: &m2_generation_state::GenerationPointerState,
              current_release: &LocalReleaseManifest,
              current_loaded: &LoadedLocalGeneration,
          ) -> Result<bool, LocalProductError> {
              let Some(provenance) = parse_provenance(&current_loaded.manifest.creation_metadata)?
              else {
                  return Ok(false);
              };
              if provenance.release_sequence != current_release.release_sequence
                  || provenance.update_key != before.update_key
              {
                  return Err(LocalProductError::Release(
                      "local-derived provenance does not match authenticated activation state",
                  ));
              }
              Ok(true)
          }

          fn authenticated_public_baseline(
              roots: &LocalCoreRoots,
              before: &m2_generation_state::GenerationPointerState,
              current_release: &LocalReleaseManifest,
              current_loaded: &LoadedLocalGeneration,
          ) -> Result<PublicBaseline, LocalProductError> {
              if let Some(provenance) = parse_provenance(&current_loaded.manifest.creation_metadata)? {
                  if provenance.release_sequence != current_release.release_sequence
                      || provenance.update_key != before.update_key
                  {
                      return Err(LocalProductError::Release(
                          "local-derived provenance does not match authenticated activation state",
                      ));
                  }
                  return Ok(provenance);
              }
              let manifest = roots
                  .generation_root
                  .join(&before.current)
                  .join("release.manifest");
              Ok(PublicBaseline {
                  release_sequence: current_release.release_sequence,
                  update_key: before.update_key,
                  release_manifest_sha256: openssl_sha256(&roots.openssl, &manifest)?,
              })
          }

          pub(super) fn admit_signed_candidate(
              source_dir: &std::path::Path,
              roots: &LocalCoreRoots,
              before: &m2_generation_state::GenerationPointerState,
              current_release: &LocalReleaseManifest,
              current_loaded: &LoadedLocalGeneration,
              source_release: &LocalReleaseManifest,
              source_loaded: &LoadedLocalGeneration,
          ) -> Result<bool, LocalProductError> {
              let baseline =
                  authenticated_public_baseline(roots, before, current_release, current_loaded)?;
              if source_release.release_sequence < baseline.release_sequence {
                  return Err(LocalProductError::ReleaseSequenceRollback);
              }
              if source_release.release_sequence > baseline.release_sequence {
                  return Ok(false);
              }
              if current_is_local_derived(before, current_release, current_loaded)? {
                  let source_digest = openssl_sha256(
                      &roots.openssl,
                      &source_dir.join("release.manifest"),
                  )?;
                  if source_digest == baseline.release_manifest_sha256 {
                      return Ok(false);
                  }
                  return Err(LocalProductError::ReleaseSequenceRollback);
              }
              if source_loaded.generation_id == before.current && source_release == current_release {
                  return Ok(true);
              }
              Err(LocalProductError::ReleaseSequenceRollback)
          }

          pub(super) fn plan_pointer_state(
              before: &m2_generation_state::GenerationPointerState,
              generation_id: &str,
              verifier_key: ReleasePublicKey,
          ) -> m2_generation_state::GenerationPointerState {
              m2_generation_state::GenerationPointerState {
                  update_key: before.update_key,
                  current: generation_id.to_owned(),
                  current_key: verifier_key,
                  previous: Some(before.current.clone()),
                  previous_key: Some(before.current_key),
              }
          }

          fn verify_official_archive_digest(
              observed: &str,
              expected: &str,
          ) -> Result<(), LocalProductError> {
              if observed == expected && valid_sha256(observed) {
                  Ok(())
              } else {
                  Err(LocalProductError::LocalUpdate(
                      "official upstream archive digest does not match release metadata",
                  ))
              }
          }

          pub(super) struct EphemeralSigningKey {
              path: std::path::PathBuf,
              public_key: ReleasePublicKey,
          }

          impl EphemeralSigningKey {
              pub(super) fn path(&self) -> &std::path::Path {
                  &self.path
              }

              pub(super) fn public_key(&self) -> ReleasePublicKey {
                  self.public_key
              }

              fn destroy(mut self) -> Result<ReleasePublicKey, LocalProductError> {
                  let key = self.public_key;
                  std::fs::remove_file(&self.path).map_err(|source| LocalProductError::Io {
                      operation: "remove ephemeral local-derived signing key",
                      source,
                  })?;
                  self.path = std::path::PathBuf::new();
                  Ok(key)
              }
          }

          impl Drop for EphemeralSigningKey {
              fn drop(&mut self) {
                  if !self.path.as_os_str().is_empty() {
                      let _ = std::fs::remove_file(&self.path);
                  }
              }
          }

          pub(super) fn generate_ephemeral_signing_key(
              openssl: &std::path::Path,
              staging_root: &std::path::Path,
          ) -> Result<EphemeralSigningKey, LocalProductError> {
              use std::os::unix::fs::{OpenOptionsExt as _, PermissionsExt as _};

              let path = staging_root.join("ephemeral-local-derived-signing-key.pem");
              let output = std::fs::OpenOptions::new()
                  .write(true)
                  .create_new(true)
                  .mode(0o600)
                  .open(&path)
                  .map_err(|source| LocalProductError::Io {
                      operation: "create ephemeral local-derived signing key",
                      source,
                  })?;
              let status = std::process::Command::new(openssl)
                  .args(["genpkey", "-algorithm", "ED25519"])
                  .env_clear()
                  .stdout(std::process::Stdio::from(output.try_clone().map_err(|source| {
                      LocalProductError::Io {
                          operation: "duplicate ephemeral signing key file",
                          source,
                      }
                  })?))
                  .stderr(std::process::Stdio::null())
                  .status()
                  .map_err(|source| LocalProductError::Io {
                      operation: "generate ephemeral local-derived signing key",
                      source,
                  })?;
              if !status.success() {
                  return Err(LocalProductError::LocalUpdate(
                      "ephemeral local-derived signing key generation failed",
                  ));
              }
              output.sync_all().map_err(|source| LocalProductError::Io {
                  operation: "sync ephemeral local-derived signing key",
                  source,
              })?;
              let metadata = std::fs::symlink_metadata(&path).map_err(|source| {
                  LocalProductError::Io {
                      operation: "inspect ephemeral local-derived signing key",
                      source,
                  }
              })?;
              if !metadata.file_type().is_file() || metadata.permissions().mode() & 0o7777 != 0o600 {
                  return Err(LocalProductError::LocalUpdate(
                      "ephemeral local-derived signing key is not a private 0600 regular file",
                  ));
              }
              let public_key = release_public_key_from_private_pem(openssl, &path)?;
              Ok(EphemeralSigningKey { path, public_key })
          }

          fn activate_built_source(
              source_dir: &std::path::Path,
              expected_before: &m2_generation_state::GenerationPointerState,
              baseline: &PublicBaseline,
              verifier_key: ReleasePublicKey,
              roots: &LocalCoreRoots,
              process_env: &TermuxProcessEnvSnapshot,
          ) -> Result<String, LocalProductError> {
              let (source_release, source_loaded) =
                  verify_local_release_bundle_with_key(source_dir, &roots.openssl, verifier_key)?;
              if source_release.release_sequence != baseline.release_sequence
                  || source_release.release_public_key != verifier_key
              {
                  return Err(LocalProductError::Release(
                      "local-derived signed bundle does not match its authenticated baseline",
                  ));
              }
              let provenance = parse_provenance(&source_loaded.manifest.creation_metadata)?
                  .ok_or(LocalProductError::Release(
                      "local-derived generation is missing signed provenance",
                  ))?;
              if &provenance != baseline {
                  return Err(LocalProductError::Release(
                      "local-derived signed provenance changed during construction",
                  ));
              }
              if source_loaded.manager_path.is_some() && source_loaded.core_path.is_none() {
                  return Err(LocalProductError::ReleasePolicy(
                      "new Manager-bearing releases require a coordinated Core artifact",
                  ));
              }
              let generation_id = stage_local_generation(source_dir, &roots.generation_root)?;
              let (_, staged_loaded) = verify_installed_local_release(
                  roots,
                  &generation_id,
                  verifier_key,
                  "local-derived staged generation descriptor id does not match state",
              )?;
              std::fs::create_dir_all(&roots.config_dir).map_err(|source| LocalProductError::Io {
                  operation: "create Core config directory",
                  source,
              })?;
              probe_release_candidate(&staged_loaded, roots, process_env)?;

              let state_paths = m2_generation_state::CoreStatePaths::new(&roots.state_root)
                  .map_err(LocalProductError::StateFormat)?;
              m2_generation_state::prepare_core_state_paths(&state_paths)
                  .map_err(LocalProductError::State)?;
              let after = plan_pointer_state(expected_before, &generation_id, verifier_key);
              let lock = m2_generation_state::acquire_activation_lock(&state_paths)
                  .map_err(LocalProductError::State)?;
              let authoritative = m2_generation_state::read_pointer_state(&state_paths)
                  .map_err(LocalProductError::State)?;
              if authoritative.as_ref() != Some(expected_before) {
                  return Err(LocalProductError::State(
                      m2_generation_state::ActivationTransactionError::StaleAuthoritativeState,
                  ));
              }
              let (_, active_loaded) = verify_installed_local_release(
                  roots,
                  &expected_before.current,
                  expected_before.current_key,
                  "active generation descriptor id does not match current",
              )?;
              let active_core_binding =
                  active_core_entrypoint_binding_with_state(roots, &active_loaded, expected_before)?;
              let rollback = if let Some(core_path) = staged_loaded.core_path.as_ref() {
                  let candidate_digest = &staged_loaded.manifest.core_artifact_digest;
                  if active_core_binding
                      .as_ref()
                      .is_some_and(|binding| binding.digest == *candidate_digest)
                  {
                      None
                  } else {
                      let snapshot_generation = active_core_binding
                          .as_ref()
                          .map(|binding| binding.generation_id.as_str())
                          .unwrap_or(expected_before.current.as_str());
                      let record = snapshot_core_entrypoint_to_rollback(roots, snapshot_generation)?;
                      if let Err(error) = install_core_entrypoint(roots, core_path, candidate_digest) {
                          let _ = install_core_entrypoint(
                              roots,
                              &core_rollback_path(roots),
                              &record.digest,
                          );
                          return Err(error);
                      }
                      Some(record)
                  }
              } else {
                  None
              };
              let mut io = m2_generation_state::FsActivationIo;
              let state_result = m2_generation_state::activate_pointer_state_locked(
                  &state_paths,
                  Some(expected_before),
                  &after,
                  &mut io,
                  &lock,
              );
              if let Err(error) = state_result {
                  let authoritative = m2_generation_state::read_pointer_state(&state_paths)
                      .map_err(LocalProductError::State)?;
                  if authoritative.as_ref() != Some(&after) {
                      if let Some(record) = rollback.as_ref() {
                          install_core_entrypoint(
                              roots,
                              &core_rollback_path(roots),
                              &record.digest,
                          )?;
                      }
                  }
                  return Err(LocalProductError::State(error));
              }
              drop(lock);
              Ok(generation_id)
          }

          pub(super) fn activate(
              roots: &LocalCoreRoots,
              process_env: &TermuxProcessEnvSnapshot,
          ) -> Result<String, LocalProductError> {
              let metadata_root = create_local_update_staging_root(&roots.state_root)?;
              let metadata = resolve_official_release_metadata(roots, &metadata_root);
              let metadata_cleanup = std::fs::remove_dir_all(&metadata_root).map_err(|source| {
                  LocalProductError::Io {
                      operation: "remove local-derived metadata staging",
                      source,
                  }
              });
              let metadata = match (metadata, metadata_cleanup) {
                  (_, Err(error)) => return Err(error),
                  (Err(error), Ok(())) => return Err(error),
                  (Ok(metadata), Ok(())) => metadata,
              };

              let staging_root = create_local_update_staging_root(&roots.state_root)?;
              let result = (|| {
                  let state_paths = m2_generation_state::CoreStatePaths::new(&roots.state_root)
                      .map_err(LocalProductError::StateFormat)?;
                  let before = m2_generation_state::recover_activation_state(&state_paths)
                      .map_err(LocalProductError::State)?
                      .ok_or(LocalProductError::NoCurrentGeneration)?;
                  let (current_release, current_loaded) = verify_installed_local_release(
                      roots,
                      &before.current,
                      before.current_key,
                      "active generation descriptor id does not match current",
                  )?;
                  let baseline = authenticated_public_baseline(
                      roots,
                      &before,
                      &current_release,
                      &current_loaded,
                  )?;
                  let archive = staging_root.join(UPSTREAM_PACKAGE_ASSET);
                  let archive_digest = codex_release_builder::fetch_archive(
                      &metadata.version,
                      &roots.curl,
                      &roots.openssl,
                      &archive,
                  )
                  .map_err(|_| {
                      LocalProductError::LocalUpdate(
                          "official upstream archive acquisition failed",
                      )
                  })?;
                  verify_official_archive_digest(&archive_digest, &metadata.package_sha256)?;
                  let core = std::fs::canonicalize(std::env::current_exe().map_err(|source| {
                      LocalProductError::Io {
                          operation: "resolve running Core artifact",
                          source,
                      }
                  })?)
                  .map_err(|source| LocalProductError::Io {
                      operation: "resolve running Core artifact",
                      source,
                  })?;
                  let gzip = roots
                      .curl
                      .parent()
                      .ok_or(LocalProductError::LocalUpdate(
                          "Termux tool directory is unavailable",
                      ))?
                      .join("gzip");
                  let generation_id = local_update_generation_id();
                  let unsigned_generation = staging_root.join("unsigned-generation");
                  let creation_metadata = render_provenance(&baseline);
                  codex_release_builder::build_generation_with_manager(
                      &metadata.version,
                      &archive,
                      &archive_digest,
                      &generation_id,
                      &core,
                      current_loaded.manager_path.as_deref(),
                      &creation_metadata,
                      &gzip,
                      &roots.openssl,
                      &unsigned_generation,
                  )
                  .map_err(|_| {
                      LocalProductError::LocalUpdate("local-derived upstream adaptation failed")
                  })?;
                  let signing_key =
                      generate_ephemeral_signing_key(&roots.openssl, &staging_root)?;
                  let verifier_key = signing_key.public_key();
                  let release_base = format!(
                      "https://{LOCAL_RELEASE_BASE_HOST}/releases/{generation_id}/"
                  );
                  let publication = staging_root.join("local-derived-publication");
                  codex_release_builder::publish_generation(
                      &unsigned_generation,
                      &baseline.release_sequence.to_string(),
                      &release_base,
                      signing_key.path(),
                      &roots.openssl,
                      &publication,
                  )
                  .map_err(|_| {
                      LocalProductError::LocalUpdate("local-derived signing failed")
                  })?;
                  let verifier_key_after_delete = signing_key.destroy()?;
                  if verifier_key_after_delete != verifier_key {
                      return Err(LocalProductError::LocalUpdate(
                          "ephemeral local-derived verifier changed during cleanup",
                      ));
                  }
                  let signed_source = publication.join("releases").join(&generation_id);
                  activate_built_source(
                      &signed_source,
                      &before,
                      &baseline,
                      verifier_key,
                      roots,
                      process_env,
                  )
              })();
              let cleanup = std::fs::remove_dir_all(&staging_root).map_err(|source| {
                  LocalProductError::Io {
                      operation: "remove private local-derived staging",
                      source,
                  }
              });
              match (result, cleanup) {
                  (Err(error), _) => Err(error),
                  (Ok(_), Err(error)) => Err(error),
                  (Ok(generation_id), Ok(())) => Ok(generation_id),
              }
          }
          RUST

          python3 <<'PY'
          from pathlib import Path
          p = Path('crates/core/src/main.rs')
          s = p.read_text()
          def rep(old, new, n=1):
              global s
              count = s.count(old)
              if count != n:
                  raise SystemExit(f'expected {n} matches, found {count}: {old[:80]!r}')
              s = s.replace(old, new, n)

          rep('#[cfg(unix)]\nmod rollback_guard;\n', '#[cfg(unix)]\nmod rollback_guard;\n\n#[cfg(unix)]\nmod local_derived;\n')

          old = '''    if source_release.release_sequence < current_release.release_sequence {
              return Err(LocalProductError::ReleaseSequenceRollback);
          }
          let validated_hold = rollback_guard::effective_update_hold(roots)?;
          if source_release.release_sequence == current_release.release_sequence {
              if source_loaded.generation_id == before.current && source_release == current_release {
                  if hold_policy == UpdateHoldPolicy::ForceHeld {
                      let hold = validated_hold
                          .as_ref()
                          .ok_or(LocalProductError::UpdateHold(
                              "forced update requires an active rollback hold",
                          ))?;
                      if source_release.release_sequence != hold.release_sequence {
                          return Err(LocalProductError::UpdateHold(
                              "forced update candidate does not match the held release sequence",
                          ));
                      }
                  }
                  return Ok(PreparedSignedLocalRelease::AlreadyCurrent(before.current));
              }
              return Err(LocalProductError::ReleaseSequenceRollback);
          }
          '''
          new = '''    let already_current = local_derived::admit_signed_candidate(
              source_dir,
              roots,
              &before,
              &current_release,
              &source_loaded,
              &source_release,
              &source_loaded,
          )?;
          let validated_hold = rollback_guard::effective_update_hold(roots)?;
          if already_current {
              if hold_policy == UpdateHoldPolicy::ForceHeld {
                  let hold = validated_hold
                      .as_ref()
                      .ok_or(LocalProductError::UpdateHold(
                          "forced update requires an active rollback hold",
                      ))?;
                  if source_release.release_sequence != hold.release_sequence {
                      return Err(LocalProductError::UpdateHold(
                          "forced update candidate does not match the held release sequence",
                      ));
                  }
              }
              return Ok(PreparedSignedLocalRelease::AlreadyCurrent(before.current));
          }
          '''
          rep(old, new)

          rep('''            let (generation_id, publication) = activate_local_built_update(roots, process_env)?;
                      let upload = publish_local_update_to_github(roots, &publication, &generation_id);
                      Ok((SignedUpdateOutcome::Activated(generation_id), true, upload))
          ''', '''            let generation_id = local_derived::activate(roots, process_env)?;
                      Ok((
                          SignedUpdateOutcome::Activated(generation_id),
                          true,
                          GithubPublicationOutcome::Skipped,
                      ))
          ''')

          rep('''    let (target_release, target_loaded) = verify_installed_local_release(
              roots,
              &after.current,
              after.current_key,
              "rollback generation descriptor id does not match previous",
          )?;
          ''', '''    let (target_release, target_loaded) = verify_installed_local_release(
              roots,
              &after.current,
              after.current_key,
              "rollback generation descriptor id does not match previous",
          )?;
              let local_derived_current = local_derived::current_is_local_derived(
                  &before,
                  &current_release,
                  &current_loaded,
              )?;
          ''')
          rep('''    let hold = UpdateHoldRecord {
              generation_id: before.current.clone(),
              release_sequence: current_release.release_sequence,
          };
              let guard = if current_loaded.core_path.is_some() {
          ''', '''    let hold = (!local_derived_current).then(|| UpdateHoldRecord {
              generation_id: before.current.clone(),
              release_sequence: current_release.release_sequence,
          });
              let guard = if !local_derived_current && current_loaded.core_path.is_some() {
          ''')
          rep('''    if let Err(error) = write_update_hold_locked(roots, &hold) {
              return Err(LocalProductError::RollbackHoldAfterCommit(Box::new(error)));
          }
          ''', '''    if let Some(hold) = hold.as_ref() {
              if let Err(error) = write_update_hold_locked(roots, hold) {
                  return Err(LocalProductError::RollbackHoldAfterCommit(Box::new(error)));
              }
          }
          ''')

          rep('''const UPDATE_USAGE: &str =
              "usage: codex update [--help] | --force | --local <DIRECTORY> | --remote <HTTPS_BASE_URL> | --rollback";
          ''', '''const UPDATE_USAGE: &str =
              "usage: codex update [--help] | --force | --build-local | --local <DIRECTORY> | --remote <HTTPS_BASE_URL> | --rollback";
          ''')
          rep('''    let local = args.len() == 2 && args[0] == OsStr::new("--local") && !args[1].is_empty();
              let remote = args.len() == 2 && args[0] == OsStr::new("--remote") && !args[1].is_empty();
              let rollback = args.len() == 1 && args[0] == OsStr::new("--rollback");
              if !local && !remote && !rollback {
          ''', '''    let build_local = args.len() == 1 && args[0] == OsStr::new("--build-local");
              let local = args.len() == 2 && args[0] == OsStr::new("--local") && !args[1].is_empty();
              let remote = args.len() == 2 && args[0] == OsStr::new("--remote") && !args[1].is_empty();
              let rollback = args.len() == 1 && args[0] == OsStr::new("--rollback");
              if !build_local && !local && !remote && !rollback {
          ''')
          rep('''    if rollback {
              return run_core_rollback();
          }
              let process_env = capture_termux_process_env();
              let result = if local {
          ''', '''    if rollback {
              return run_core_rollback();
          }
              let process_env = capture_termux_process_env();
              if build_local {
                  return match local_derived::activate(&roots, &process_env) {
                      Ok(generation_id) => {
                          println!("activated local-derived generation {generation_id}");
                          0
                      }
                      Err(err) => {
                          eprintln!("codex update --build-local: {err}");
                          1
                      }
                  };
              }
              let result = if local {
          ''')
          s = s.replace('activated local-built generation {generation_id}', 'activated local-derived generation {generation_id}')
          s = s.replace('#[cfg(unix)]\nfn activate_local_built_update(', '#[cfg(unix)]\n#[allow(dead_code)]\nfn activate_local_built_update(', 1)
          s = s.replace('#[cfg(unix)]\nfn publish_local_update_to_github(', '#[cfg(unix)]\n#[allow(dead_code)]\nfn publish_local_update_to_github(', 1)
          # Add child regression module inside the existing test module.
          pos = s.rfind('\n}')
          if pos < 0:
              raise SystemExit('test module closing brace not found')
          s = s[:pos] + '\n\n    #[cfg(unix)]\n    mod rald_tests;\n' + s[pos:]
          p.write_text(s)
          PY

          mkdir -p crates/core/src/tests
          cat > crates/core/src/tests/rald_tests.rs <<'RUST'
          use super::*;
          use std::os::unix::fs::PermissionsExt as _;

          #[test]
          fn rald1_provenance_is_canonical_and_fail_closed() {
              let baseline = local_derived::PublicBaseline {
                  release_sequence: 7,
                  update_key: ReleasePublicKey([0x44; 32]),
                  release_manifest_sha256: "ab".repeat(32),
              };
              let rendered = local_derived::render_provenance(&baseline);
              assert_eq!(local_derived::parse_provenance(&rendered).unwrap(), Some(baseline));
              assert!(local_derived::parse_provenance("ordinary-official-metadata")
                  .unwrap()
                  .is_none());
              assert!(local_derived::parse_provenance("codex-local-derived-v1;baseline_sequence=7")
                  .is_err());
              assert!(local_derived::parse_provenance(
                  "codex-local-derived-v1;baseline_sequence=7;baseline_update_key=zz;baseline_manifest_sha256=00"
              )
              .is_err());
          }

          #[test]
          fn rald1_local_pointer_preserves_official_forward_authority_and_public_sequence() {
              let official = ReleasePublicKey([0x11; 32]);
              let local = ReleasePublicKey([0x22; 32]);
              let before = GenerationPointerState {
                  update_key: official,
                  current: "official-7".to_owned(),
                  current_key: official,
                  previous: Some("official-6".to_owned()),
                  previous_key: Some(official),
              };
              let after = local_derived::plan_pointer_state(&before, "local-derived-7", local);
              assert_eq!(after.update_key, official);
              assert_eq!(after.current_key, local);
              assert_eq!(after.previous.as_deref(), Some("official-7"));
              assert_eq!(after.previous_key, Some(official));
          }

          #[test]
          fn rald1_ephemeral_private_key_is_0600_and_drop_cleans_it() {
              let root = temp_root("rald1-ephemeral-key");
              let openssl = b4_termux_openssl();
              let key = local_derived::generate_ephemeral_signing_key(&openssl, &root).unwrap();
              let path = key.path().to_owned();
              let metadata = std::fs::symlink_metadata(&path).unwrap();
              assert!(metadata.file_type().is_file());
              assert_eq!(metadata.permissions().mode() & 0o7777, 0o600);
              drop(key);
              assert!(!path.exists());
              std::fs::remove_dir_all(root).unwrap();
          }

          #[test]
          fn rald1_build_local_selector_is_exact_and_combined_flags_are_usage_errors() {
              assert_eq!(run_core_update(vec!["--build-local".into(), "x".into()]), 2);
              assert_eq!(run_core_update(vec!["--build-local".into(), "--force".into()]), 2);
              assert_eq!(run_core_update(vec!["--build-local".into(), "--rollback".into()]), 2);
              assert!(UPDATE_USAGE.contains("--build-local"));
          }

          #[test]
          fn rald1_local_derived_source_has_no_official_private_key_or_github_publication_surface() {
              let source = include_str!("../local_derived.rs");
              for forbidden in [
                  "configured_update_private_key",
                  "UPDATE_PRIVATE_KEY_ENV",
                  "publish_local_update_to_github",
                  "github_cli_path",
                  "github_command",
                  " gh ",
              ] {
                  assert!(!source.contains(forbidden), "forbidden local-derived dependency: {forbidden}");
              }
              assert!(source.contains("codex_release_builder::fetch_archive"));
              assert!(source.contains("metadata.package_sha256"));
              assert!(source.contains("signing_key.destroy()?"));
          }

          #[test]
          fn rald1_plain_transport_fallback_uses_local_derived_and_never_publication() {
              let source = include_str!("../main.rs");
              let marker = "Err(LocalProductError::RemoteTransportFailed)";
              let start = source.find(marker).unwrap();
              let tail = &source[start..source.len().min(start + 900)];
              assert!(tail.contains("local_derived::activate"));
              assert!(tail.contains("GithubPublicationOutcome::Skipped"));
              assert!(!tail.contains("publish_local_update_to_github"));
          }
          RUST

          cat >> SPEC.md <<'DOC'

          ## RALD-1 local-derived update contract

          `codex update --build-local` is the public local-derived selector. It accepts no path or
          other selector and is never an alias for `--force`, `--local`, official signing, or
          publication. The builder MUST resolve only official OpenAI stable metadata, fetch the
          corresponding Android/AArch64 upstream archive, and require its exact SHA-256 to equal
          the official metadata before adaptation.

          A local-derived generation MUST be signed only with a fresh device-local ephemeral
          Ed25519 key created inside a private `0700` staging root. Its private PEM MUST be a
          `0600` regular file and MUST be deleted before activation; failure paths MUST clean it
          best-effort through the same bounded staging lifetime. The private key MUST NOT enter
          activation state, an installed generation, cache, publication tree, or log. Only the
          ephemeral public verifier may become that generation's `current_key`.

          `activation-state-v3.update_key` remains the official forward-update authority and MUST
          remain byte-identical across local-derived activation. The local-derived signed
          `generation.meta` MUST carry canonical `codex-local-derived-v1` provenance binding the
          authenticated public baseline sequence, the official `update_key`, and the SHA-256 of
          the authenticated baseline `release.manifest`. The local-derived release manifest uses
          that baseline public sequence unchanged; it does not allocate or consume a synthetic
          public sequence. Malformed provenance or provenance inconsistent with authenticated
          state fails closed.

          Signed stable admission while local-derived is current compares against that recorded
          public baseline, not a synthetic local sequence. A signed candidate above the baseline
          advances normally. The exact authenticated baseline manifest may restore official
          current at the same sequence. Other same/lower-sequence candidates remain rollback
          failures, and rollback-hold/`--force` comparison remains independently enforced.
          Official candidate verification and any accepted official key rotation continue to use
          `activation-state-v3.update_key`.

          Local-derived activation uses the existing probe plus atomic current/previous generation
          transaction while preserving `update_key`. Rolling back from a valid local-derived
          current to its retained previous generation MUST NOT create a public update hold or the
          legacy rollback-Core hold guard. Rolling back from an official current retains the
          existing authenticated hold/guard semantics unchanged. `codex update --force` remains
          limited to retrying the exact authenticated held public sequence and never targets a
          local-derived generation.

          If the ordinary signed-channel update encounters the accepted transport-unavailable
          fallback condition, that fallback MUST invoke the same local-derived builder and MUST
          report publication skipped. It MUST NOT inspect an official private signing-key path,
          invoke `gh`, create a GitHub Release, update Pages/main/stable index, or change behavior
          merely because maintainer credentials exist on the device. The legacy device-side
          official producer helpers remain non-public implementation residue pending RALD-2 and
          are not reachable from the local-derived selector or transport fallback.
          DOC

          cargo fmt --all
          git diff --check
      - name: Focused RALD-1 and ARH regression gates
        shell: bash
        run: |
          set -euo pipefail
          cargo test -p codex --locked rald1 -- --nocapture
          cargo test -p codex --locked rollback_hold -- --nocapture
          cargo test -p codex --locked force -- --nocapture
          cargo test -p codex --locked rollback_guard -- --nocapture
      - name: Full acceptance gates
        shell: bash
        run: |
          set -euo pipefail
          cargo test -p codex-release-builder --locked
          cargo test -p codex --locked
          cargo test -p codex-manager --locked
          cargo check --workspace --locked
          cargo test --workspace --locked
          cargo clippy --workspace --all-targets --locked -- -D warnings
          cargo fmt --all -- --check
          git diff --check
      - name: Record proven RALD-1 evidence
        shell: bash
        run: |
          set -euo pipefail
          cat >> GOAL.md <<'DOC'

          ## 2026-09-15 — RALD-1 local-derived Core contract accepted evidence

          RALD-1 is implemented on `rewrite/rust-core`: `codex update --build-local` is an exact
          local-derived selector; the transport-unavailable plain-update fallback uses the same
          path; local-derived construction is bound to official OpenAI stable metadata/archive
          digest, uses a private ephemeral Ed25519 signer deleted before activation, preserves the
          official `activation-state-v3.update_key`, carries signed public-baseline provenance,
          consumes no new public sequence, admits later official signed stable against that
          baseline, and suppresses public rollback hold/legacy hold guard only when the current
          generation is authenticated local-derived. Existing official rollback/hold/guard and
          exact-held-sequence `--force` semantics remain covered by the unchanged regression suite.

          Accepted evidence in the implementation runner: focused RALD-1 tests, rollback-hold,
          force, and rollback-guard regressions; release-builder full tests; Core full tests;
          Manager tests; workspace check/test/clippy with warnings denied; rustfmt check; and
          `git diff --check` all passed from a clean checkout of the frozen RALD-0 parent.
          DOC
          cat >> WORKBOARD.md <<'DOC'

          ## RALD status — 2026-09-15

          - RALD-1 — local-derived Core contract: **done / gated**.
          - Accepted scope: `--build-local`, ephemeral local integrity authority, signed
            public-baseline provenance, official `update_key` preservation, sequence-neutral local
            activation, official supersession/restoration admission, local-derived rollback without
            a public hold, and transport fallback reuse without GitHub publication.
          - Existing official rollback/hold/guard/`--force` regressions and full workspace gates
            passed in the isolated implementation runner.
          - Next active phase: **RALD-2 — detach official production from Core `codex update`**.
            Remove/detach the now-unreachable ambient device producer/publisher wiring and expose
            explicit non-installed producer tooling without changing signed consumer semantics.
          DOC
          cat >> RELEASE_AUTOMATION_PLAN.md <<'DOC'

          ## Phase status — 2026-09-15

          - RALD-0: complete.
          - RALD-1: complete; local-derived Core contract implemented and gated without changing
            the frozen decisions above.
          - Next: RALD-2 — detach official production from Core `codex update`.
          DOC
          git diff --check
          cargo fmt --all -- --check
      - name: Commit and fast-forward push implementation branch
        shell: bash
        env:
          TARGET_BRANCH: rewrite/rust-core
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = 771d39f9337a7c15337fedc6b47b43d017d8140c
          git fetch origin "$TARGET_BRANCH"
          test "$(git rev-parse origin/$TARGET_BRANCH)" = 771d39f9337a7c15337fedc6b47b43d017d8140c
          test -n "$(git status --porcelain)"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add SPEC.md GOAL.md WORKBOARD.md RELEASE_AUTOMATION_PLAN.md crates/core/src/main.rs crates/core/src/local_derived.rs crates/core/src/tests/rald_tests.rs
          git diff --cached --check
          git commit -m "core: implement RALD-1 local-derived updates"
          commit=$(git rev-parse HEAD)
          test "$(git rev-parse HEAD^)" = 771d39f9337a7c15337fedc6b47b43d017d8140c
          git push origin "HEAD:$TARGET_BRANCH"
          git fetch origin "$TARGET_BRANCH"
          test "$(git rev-parse origin/$TARGET_BRANCH)" = "$commit"
          test -z "$(git status --porcelain)"
          echo "RALD1_COMMIT=$commit" >> "$GITHUB_ENV"
      - name: Clean temporary session branch
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          git push origin --delete rald1-impl-session-20260915
